words = ["The", "dog", "runs"]

tags = ["NN", "NN", "NN"]

# Transformation rule:
# If word ends with 's', change tag to VERB

for i in range(len(words)):

    if words[i].endswith("s"):
        tags[i] = "VB"

print(list(zip(words, tags)))
