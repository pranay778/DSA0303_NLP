import nltk
from nltk.stem import PorterStemmer

stemmer = PorterStemmer()

words = ["running", "studies", "playing", "happily", "jumped"]

for word in words:
    print(word, "->", stemmer.stem(word))
